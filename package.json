{
  "name": "music_mates",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "meteor": "meteor --settings settings.json",
    "meteor2": "MONGO_URL=mongodb://localhost:3001/chimp_db meteor --port 3100 --settings settings.json $@",
    "test": "chimp --ddp=http://localhost:3000 --mocha || exit 0",
    "test2": "chimp --ddp=http://localhost:3100 --mocha || exit 0",
    "test2-watch": "chimp --ddp=http://localhost:3100 --mocha --watch || exit 0",
    "test-watch": "chimp --ddp=http://localhost:3000 --mocha --watch || exit 0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Tomy8s/music_mates.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Tomy8s/music_mates/issues"
  },
  "homepage": "https://github.com/Tomy8s/music_mates#readme",
  "dependencies": {
    "babel-runtime": "^6.20.0",
    "bcrypt": "^1.0.1"
  }
}
